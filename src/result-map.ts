export interface ResultMap {
  examples: Array<[string, Array<string>, string]>;
  definitions: Array<[string, Array<string>, string]>;
  etymologies: Array<[string, string]>;
  related: Array<[string, Array<string>, string]>;
  pronunciations: Array<[string, Array<string>, Array<string>]>;
}
